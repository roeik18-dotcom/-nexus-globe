# Background service dependencies (service/merlin_service.py)
# Install AFTER requirements.txt
# macOS: brew install portaudio  (if sounddevice fails to install)
sounddevice>=0.4.7
numpy>=1.26.0
scipy>=1.13.0
python-dotenv>=1.0.0
